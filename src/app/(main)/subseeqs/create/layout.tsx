import { noindexMetadata } from '@/lib/seo';

export const metadata = noindexMetadata('Create Subseeq');

export default function CreateSubseeqLayout({ children }: { children: React.ReactNode }) {
  return children;
}
